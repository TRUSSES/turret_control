# Command message for zipper control
float64 desired_length   # Desired extension length in meters
float64 desired_velocity # Desired velocity in m/s (linear velocity)
float64 desired_pitch_angle # Desired pitch angle in radians
bool hold_current_pitch    # Set true to ignore desired_pitch_angle and hold current pitch
